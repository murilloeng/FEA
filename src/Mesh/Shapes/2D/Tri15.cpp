//FEA
#include "FEA/inc/Mesh/Shapes/2D/Tri15.hpp"

//static
static const double A[] = {
	 +0,  +0,  +0,  +0,  +0,  +0,  +0,  +6,  +0,  +0,  +0,  +0,   +0,   +0,   +0, 
	 +3,  -1,  +0,  -8,  +6,  +0,  +8, +18,  +0,  +0,  +6,  -8,  +24,  -24,  -24, 
	 +3,  +0,  -1,  -8,  +6,  +0,  +0, +18,  +8,  +0,  +6,  -8,  +24,  -24,  -24, 
	+11,  -1,  +0, -32, +30,  -8, +24, +12,  +0,  +0, +12, -24,  +72,  -72,  -24, 
	+22,  +0,  +0, -56, +42,  -8,  +8, +54,  +8,  -8, +42, -56, +144,  -96,  -96, 
	+11,  +0,  -1, -24, +12,  +0,  +0, +12, +24,  -8, +30, -32,  +72,  -24,  -72, 
	+12,  +4,  +0, -40, +48, -24, +16,  +0,  +0,  +0,  +0, -16,  +48,  -48,   +0, 
	+36,  +0,  +0, -96, +84, -24, +24, +36,  +0,  +0, +36, -72, +168, -120,  -72, 
	+36,  +0,  +0, -72, +36,  +0,  +0, +36, +24, -24, +84, -96, +168,  -72, -120, 
	+12,  +0,  +4, -16,  +0,  +0,  +0,  +0, +16, -24, +48, -40,  +48,   +0,  -48, 
	 +4,  +4,  +0, -16, +24, -16,  +0,  +0,  +0,  +0,  +0,  +0,   +0,   +0,   +0, 
	+16,  +0,  +0, -48, +48, -16, +16,  +0,  +0,  +0,  +0, -16,  +48,  -48,   +0, 
	+24,  +0,  +0, -48, +24,  +0,  +0, +24,  +0,  +0, +24, -48,  +96,  -48,  -48, 
	+16,  +0,  +0, -16,  +0,  +0,  +0,  +0, +16, -16, +48, -48,  +48,   +0,  -48, 
	 +4,  +0,  +4,  +0,  +0,  +0,  +0,  +0,  +0, -16, +24, -16,   +0,   +0,   +0
};

namespace fea
{
	namespace mesh
	{
		namespace shapes
		{
			//constructor
			Tri15::Tri15(void) : Tri(3)
			{
				return;
			}

			//destructor
			Tri15::~Tri15(void)
			{
				return;
			}

			//integration
			void Tri15::function(double* N, const double* p) const
			{
				//data
				const double r = p[0];
				const double s = p[1];
				const double v[] = {
					1, r, s, r * r, r * s, s * s, r * r * r, r * r * s, r * s * s, s * s * s,
					r * r * r * r, r * r * r * s, r * r * s * s, r * s * s * s, s * s * s * s
				};
				//shape
				for(uint32_t i = 0; i < 15; i++)
				{
					N[i] = 0;
					for(uint32_t j = 0; j < 15; j++)
					{
						N[i] += A[i + 15 * j] / 6 * v[j];
					}
				}
			}
			void Tri15::gradient(double* B, const double* p) const
			{
				//data
				const double r = p[0];
				const double s = p[1];
				const double dv[] = {
					0, 1, 0, 2 * r, s, 0, 3 * r * r, 2 * r * s, s * s, 0, 4 * r * r * r, 3 * r * r * s, 2 * r * s * s, s * s * s, 0,
					0, 0, 1, 0, r, 2 * s, 0, r * r, 2 * r * s, 3 * s * s, 0, r * r * r, 2 * r * r * s, 3 * r * s * s, 4 * s * s * s
				};
				//gradient
				for(uint32_t i = 0; i < 15; i++)
				{
					B[i +  0] = B[i + 15] = 0;
					for(uint32_t j = 0; j < 15; j++)
					{
						B[i +  0] += A[i + 15 * j] / 6 * dv[j +  0];
						B[i + 15] += A[i + 15 * j] / 6 * dv[j + 15];
					}
				}
			}
		}
	}
}